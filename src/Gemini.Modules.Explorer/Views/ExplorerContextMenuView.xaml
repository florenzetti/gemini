<ContextMenu x:Class="Gemini.Modules.Explorer.Views.ExplorerContextMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:dd="clr-namespace:Gemini.Modules.Explorer.ViewModels"
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding}"
             d:DataContext="{d:DesignInstance dd:ExplorerContextMenuViewModel}">
    <ContextMenu.Resources>
        <ResourceDictionary Source="../Resources/Styles.xaml" />
    </ContextMenu.Resources>
    <ContextMenu.Style>
        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="ItemsSource" Value="{Binding}" />
            <Setter Property="ItemTemplate" Value="{StaticResource menuTemplate}" />
        </Style>
    </ContextMenu.Style>
    <!--<ContextMenu ItemsSource="{Binding}" ItemTemplate="{StaticResource menuTemplate}">
    </ContextMenu>-->
</ContextMenu>
