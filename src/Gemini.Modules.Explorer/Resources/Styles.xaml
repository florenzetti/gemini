<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Gemini.Modules.MainMenu.Controls;assembly=Gemini"
                    xmlns:converters="clr-namespace:Gemini.Modules.Shell.Converters;assembly=Gemini"
                    xmlns:b="clr-namespace:Gemini.Modules.MainMenu.Behaviors;assembly=Gemini">

    <converters:NullableValueConverter x:Key="NullableValueConverter" />
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    <Image x:Key="MenuItemIcon" x:Shared="false" Source="{Binding IconSource, Converter={StaticResource NullableValueConverter}}" />

    <HierarchicalDataTemplate x:Key="menuTemplate" ItemsSource="{Binding Children}">
        <ContentPresenter Content="{Binding Path=Text}" RecognizesAccessKey="True" />
	</HierarchicalDataTemplate>
</ResourceDictionary>
